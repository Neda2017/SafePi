# SafePi Backend (Express + TypeScript)

Routes:
- GET /
- GET /validation-key.txt
- POST /payments/complete

Render build: `npm install && npm run build`
Render start: `npm start`

Env:
PORT=10000
FRONTEND_URL=https://safepi.onrender.com
PI_APP_ID=safeedfafd9724
PI_VALIDATION_KEY=<your key>
